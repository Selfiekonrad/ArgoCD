apiVersion: v1
kind: Service
metadata: 
  name: native-service
spec:
  selector:
    app: native-app
  ports: 
  - protocol: TCP
    port: 8080
    targetPort: {{ .Values.native.container.containerPort }}
